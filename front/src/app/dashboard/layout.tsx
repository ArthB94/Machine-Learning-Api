import { ReactNode } from "react";

export const metadata = {
  title: "Game Success - Dashboard",
};
export default function ClientLayout({ children }: { children: ReactNode }) {
  return children;
}
